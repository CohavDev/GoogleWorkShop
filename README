if you want to import this code, copy only the 'app' folder into your working Expo project(blank)

installed a library for gradients using the following command:

- expo install expo-linear-gradient
  used it at ProfileMatching.js

installed a library for navigation called ReactNavigation using the following commands:

- npm install @react-navigation/native @react-navigation/native-stack
- expo install react-native-screens react-native-safe-area-context

installed for navigation:

- expo install react-native-gesture-handler react-native-reanimated react-native-screens react-native-safe-area-context @react-native-community/masked-view
- npm install react-navigation-stack

installed for NewActivity time picking:

- expo install @react-native-picker/picker
- npm install @react-native-picker/picker --save
- npm audit fix --force

installed for icons:

- npm install react-native-paper

installed for multiple select: (16/05)

- npm install react-native-multiple-select
- npm install react-native-select-multiple

for openning whatsapp app:

- expo install expo-linking

date picker:

- npm install @react-native-community/datetimepicker --save

google places :
-npm install react-native-google-places-autocomplete --save

country phone code picker:
-npm i react-native-country-codes-picker
